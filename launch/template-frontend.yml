env: 
- NODE_ENV
- HOST
- PORT
resources:
  cpu: 0.1
  max_mem: 0.1
shepherds:
- "{{.CreatorEmail}}"
expose:
- name: http
  port: 80
  bind: 80
  health_check:
    type: http
    path: /_healthcheck
team: "{{.TeamName}}"
